---
description: Publish the current curriculum to GitHub Pages — interactive walkthrough.
---

Invoke the `publish` skill. Operates on the current working directory; requires an existing TMB v0.4 curriculum, the `gh` CLI installed and authenticated, and the scaffold's `.github/workflows/deploy.yml` to be present.
