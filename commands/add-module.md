---
description: Add one module to an existing curriculum (append or insert-at-position).
argument-hint: "[optional topic or position hint]"
---

Invoke the `add-module` skill with the user's arguments: `$ARGUMENTS`.

The skill walks through a short interview to collect the new module's spec.
