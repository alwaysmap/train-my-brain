---
description: Build a structured, hands-on learning curriculum with a local Hugo site.
argument-hint: "[optional topic hint]"
---

Invoke the `create` skill with the user's arguments: `$ARGUMENTS`.

If no topic is provided, the 8-step interview will discover it.
