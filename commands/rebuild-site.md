---
description: Refresh Hugo layouts/CSS/config for an existing curriculum, then rebuild.
---

Invoke the `tmb-rebuild-site` skill. Touches only `site/layouts/`, `site/assets/css/`, `site/archetypes/`, and `site/hugo.yaml` — never `site/content/` or `modules/`.
